# Visualizations

This directory contains the Python scripts used to create the visualizations.


## Developing

Each script should create a set of visualizations and export them as PNGs into
the output directory.
