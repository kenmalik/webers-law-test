# Weber's Law Test

This project is a test of Weber's Law.

## Building

To start developing, install Python dependencies and create an `out/` directory
to export visualizations into.

If developing on Mac/Linux, you can do this like so.

```bash
python3 -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
mkdir out
```
